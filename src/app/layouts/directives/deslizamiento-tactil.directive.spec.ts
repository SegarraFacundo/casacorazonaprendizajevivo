import { DeslizamientoTactilDirective } from './deslizamiento-tactil.directive';

describe('DeslizamientoTactilDirective', () => {
  it('should create an instance', () => {
    const directive = new DeslizamientoTactilDirective();
    expect(directive).toBeTruthy();
  });
});
